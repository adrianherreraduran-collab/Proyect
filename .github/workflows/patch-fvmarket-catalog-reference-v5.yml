name: Fix FVMarket catalog reference tokenization v5

on:
  push:
    paths:
      - patch_fvmarket_catalog_reference_v5.py
      - .github/workflows/patch-fvmarket-catalog-reference-v5.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply catalog reference token fix v5
        run: python patch_fvmarket_catalog_reference_v5.py
      - name: Validate backend syntax
        run: node --check appsrc/server.js
      - name: Repack FVMarket
        run: tar -czf - -C appsrc . | base64 -w 0 > fvmarket-app.tgz.b64
      - name: Commit updated FVMarket application
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add appsrc/public/admin.html fvmarket-app.tgz.b64
          git commit -m "Corregir referencias partidas del catálogo FVMarket" || exit 0
          git push
